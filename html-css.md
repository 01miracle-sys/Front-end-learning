# html+css

## CSS选择器

#### 基础选择器

#### 关系选择器

#### 分组选择器

#### 伪类和伪元素

##### 伪类选择器

1.分组选择器和链接伪类选择器

2.用户行为伪类-hover和focus

3.结构伪类选择器及表格隔行变色效果

4.表单选择器及伪元素选择器

3.表单伪类选择器

针对表单元素的状态，使用：表单按钮禁用的时候，调整颜色；复选框选中修改样式

：disabled 表单禁用 ：checked 选中状态，单选复选按钮

一个点叫类 两个点叫伪类 四个点叫伪元素

##### 伪元素选择器

1.选定特定部分

：：fist-line 选择首行

：：first-letter 首字母

：：placeholder 选择input或textarea占位符

2.插入内容

：：before 元素内插入伪元素，作为第一个元素

：：after 元素内插入伪元素，作为最后一个元素

#### 属性选择器

使用场景：表单样式控制，图标字体控制，国际化语言适配等

####  

## CSS三大特性

1.继承性：子元素继承父级的某些样式，比如文字类

2.层叠性：如果权重相同的情况下，后面的样式会覆盖前面的样式，就近原则

3.优先级：哪个选择器权重高，优先执行哪个样式

![image-20251211204300285](.\学习文档\img\cs优先级.png)



## 盒子模型

盒子分为区块盒子和行内盒子

区块盒子（block）：盒子会产生换行，width和height可以发挥作用，不设置宽度则默认是父元素空间的100%，内边距、外边距和边框会撑大元素，常见的：div、p、h、ul、table等

行内盒子（inline）：盒子不会产生换行，width‘。。属性将不起作用，垂直方向的内边距、外边距不起效果，水平方向的内边距、外边距会有效果，常见：span、em、a、strong等

盒子组成：盒子内容、内边距padding、边框border、外边框margin

#### 1.boder 

设置盒子四条或者单独边框

属性值：边框粗细 样式 颜色

边框有三部分属性值组成，中间必须空格隔开，三部分属性值没有先后顺序  border：  1px solid #f1f1f1； （solid dashed  dotted）

border-top bottom left right

#### 2.圆角边框

border-radius 属性值；

数字/百分比

.button{border-radius: 10px;}

#### 3.内边距

padding

![image-20251211214823119](.\img\内边距.png)

#### 4.外边距

margin：外边距是盒子周围一圈看不懂的空间，会把其他元素退离盒子

![image-20251211215808039](.\img\外边距.png)

外边距折叠，两个上下外边距将合并成一个外边距，其大小等于最大的单个外边距

#### 5.尺寸计算

box-sizing

#### 6.背景常见属性

![image-20251213202306887](.\img\image-20251213202306887.png)

![image-20251213202912180](.\img\image-20251213202912180.png)

背景固定 fixed

#### 7.背景渐变

通过linear-gradient(线性渐变)和radial-gradient(径向渐变)为元素添加渐变背景

![image-20251213205914556](.\img\image-20251213205914556.png)

#### 8.盒子阴影

box-shadow

![image-20251213211616295](.\img\image-20251213211616295.png)

过渡：transition过渡属性，过渡时间,过渡写到盒子身上，谁做过渡给谁加

input：hover{

  transition:all 0.3s;

}

#### 9.样式初始化

统一浏览器默认样式，消除差异

![image-20251213212612833](.\img\image-20251213212612833.png)

![image-20251213212630600](.\img\image-20251213212630600.png)

![image-20251213233746465](.\img\image-20251213233746465.png)



#### 10.字体图标

字体图标一种将图标以字体形式嵌入网页的技术

![image-20251214191117040](.\img\image-20251214191117040.png)

#### 11.精灵图

将多个小图标或图像合并到一张大图中，通过调整background-position属性来显示特定部分的图像技术

![image-20251214230446495](.\img\image-20251214230446495.png)

tugaigai.com/onlin_ps/

## css布局

#### 1.CSS正常布局流

display 显示属性，用作元素的默认行为方式，display属性允许更改默认的显示方式此属性

display：block 转换为区块元素

display：inline 转换为行内元素

display：inline-block 转换为行内块元素

#### 3.浮动

可以让元素脱离文档流，向左或向右浮动，直到碰到父容器边缘或其他浮动元素

left 左侧浮动 float：left

right 右侧浮动 float：right

none 默认值，不浮动 float：none；

影响周围元素的布局 

父盒子没有高度（很多情况下不能给父亲指定高度），子元素浮动，影响其他盒子布局

清楚浮动可以理解为闭合浮动，就是让浮动的元素尽量控制在父盒子内，不要影响其他盒子

清除浮动方式：![image-20251216205437139](.\img\image-20251216205437139.png)

#### 4.弹性盒子

flexbox是css弹性盒子布局模块的缩写，可以快速实现元素的对齐、分布和空间分配

父控子，父盒子控制子盒子如何排列布局

父盒子称为容器，子盒子称为项目

主轴默认水平反向，决定着项目如何排列

交叉轴默认垂直方向，可以更改

容器（父盒子）属性

![image-20251216210817773](.\img\image-20251216210817773.png)

项目（子盒子）属性

![image-20251216210907978](.\img\image-20251216210907978.png)

###### 父盒子

1.容器设置display：flex，可以让子盒子按照主轴方式排列，

如果子元素有大小，则按照给定大小显示

如果子元素没有大小，则拉伸充满父容器

若子元素总宽度超过容器宽度，默认会压缩子元素

2.justify-content 定义主轴上的对齐方式

flex-start 左对齐

flex-end 右对齐

center 居中对齐

space-between 两端对齐

space-around 项目两侧间隔相等

space-evenly 项目间隔均匀分布

3.align-items 定义交叉轴上的对齐方式（交叉时整体对齐）

flex-start 项目在交叉轴起点对齐

flex-end 项目在交叉轴终点对齐

center 项目在交叉轴居中对齐

stretch 项目拉伸填充整个容器高度（需子项目无固定高度）

4。flex-direction 定义主轴方向（改变主轴方向）

![image-20251216221135056](.\img\image-20251216221135056.png)

5.flex-wrap 控制是否换行

![image-20251216223943360](.\img\image-20251216223943360.png)

align-content 定义多行时交叉轴上的对齐方式（仅当flex-wrap：wrap且内容换行时生效）

![image-20251217210632760](.\img\image-20251217210632760.png)



###### 子盒子

子元素的属性用于控制自身的尺寸、顺序或对齐方式

语法：flex：1 剩余空间占1份，并且可以伸缩盒子大小。数字表示剩余空间所占份数，正整数

gap间距 ：用于设置行与列之间的间隙（间距）

gap:20px 行和列之间保持20像素间隙

gap：20px 30px；行间距是20像素，列间距是30像素

**注意：gap写到父元素（容器身上）**

![image-20251218205931604](.\img\image-20251218205931604.png)

解决：1.把图片转换为块级元素 2.设置图片对齐方式不是基线对齐即可

img{vertical-align:top;} baseline默认，基线对齐 top 顶部对齐 middle 中部对齐 bottom 底部对齐

让文字和图片垂直居中对齐

img{ border: 0; vertical-align: middle}

#### logo

logo用h1提高权重，里面包换链接，可以直接跳转首页，给链接的背景图片，并且给链接添加title，链接里面写网站名字，把文字隐藏 -可以利用text-indent：负值，然后裁剪 overflow：hidden；

#### 定位

定位布局可以精准定位，但缺乏灵活性

定位方式：

1.相对定位（position:relative)：相对于元素在文档流中的正常位置进行定位 

2.绝对定位：相对于其最近的已定位祖先元素进行定位，不占据文档流

 3.固定定位：相对于浏览器窗口进行定位。不占据文档流，固定在屏幕上的位置，不随滚动而移动。

场景：固定导航栏，吸顶效果，弹出菜单/下拉框，悬浮效果。。。



## javascript

javascript是一种轻量级、解释型、面向对象的脚本语言。主要被设计用于在网页上实现动态效果，增加用户与网页的交互性，作为一种客户端脚本语言，javascript可以直接嵌入html，并在浏览器中执行，与html和css不同，js使得网页不再是静态的，而是可以根据用户的操作动态变化的

1.客户端脚本：用于在用户浏览器中执行，实现动态效果和用户交互。

2.网页开发：与html和css协同，使得网页具有更强的交互性和动态性

3.后端开发：使用node.js，javascript也可以在服务器端运行，实现服务器端应用的开发。
